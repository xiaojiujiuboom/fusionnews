name: Fusion Daily Run

on:
  schedule:
    # 北京时间每天早上 09:00 (即 UTC 时间 01:00)
    - cron: '0 1 * * *'
  # 👇 这一行就是“手动运行按钮”的开关，必须顶格写，不能有缩进错误
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests feedparser google-generativeai beautifulsoup4

      - name: Run Script
        env:
          # 左边是 Python 代码里的变量名，右边是 GitHub 后台保险箱的钥匙
          SERVERCHAN_SENDKEY: ${{ secrets.SERVERCHAN_SENDKEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          # 👇 这一部分必须和你截图里的 Secret 名字完全一致 (无下划线)
          GOOGLESEARCH_API_KEY: ${{ secrets.GOOGLESEARCH_API_KEY }}
          GOOGLESEARCH_CX: ${{ secrets.GOOGLESEARCH_CX }}
        run: python main.py
